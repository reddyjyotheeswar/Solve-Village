<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rgukt.myproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SolVillage"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Loginactivity" />
        <activity android:name=".AdminActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".addPresidentActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".PostProblemActivity" />
        <activity android:name=".PresidentActivity" />
        <activity android:name=".Individual_ProblemActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".UserProfileActivity"></activity>
    </application>

</manifest>